# Locked Engine

## Scope Boundary

- This file is derived only from these context files:
  - `component_syntax/04_cohabitation_vs_composition.md`
  - `component_syntax/05_minimal_semantic_execution.md`
  - `component_syntax/06_detached_fragment_roles.md`
  - `component_syntax/07_global_comparison.md`
- Those files define the currently locked baseline.
- They are not treated as proof for this pass.
- All controller and extraction claims after this file must be re-grounded in `can u dig it.csv:1-14`.

## Frozen Lower-Right Engine Findings

This pass is allowed to treat the following as already-established local findings.

### Local exact syntax families still alive

- the lower-right engine is connected, but connection does not itself prove one instruction
- exact local shell family around `step or X` survives as the strongest local syntax family
- exact local complements in that shell include:
  - `step or integer by six`
  - `step or baseten integer`
  - `step or baseten to integer`
  - `step or base ten integer`
  - `step or base ten to integer`
- exact local tail family survives:
  - `integer by six`
  - `step by six`
  - `step integer by six`
  - `step into integer by six`
  - `to integer by six`
  - `go integer by six`
- exact local representation-object family survives:
  - `binary ten integer by six`
  - `decimal ten ten ten integer by six`
  - `hexadecimal ten ten ten integer by six`
  - plus exact `... to integer` variants that keep the local syntax unresolved

### Surviving direct local outputs

- local branch-side output candidate:
  - `90` from exact `get nine tens`
  - still only provisional
- local representation-side output candidate:
  - `2` from exact `binary ten`
  - still only provisional

### Locked caution from the syntax audit

- some earlier bridge hops proved only shared-instance contact
- some proved full exact subphrase inclusion
- some remain parse-ambiguous because the same local word-instance network supports competing exact orderings
- especially important:
  - `binary get nine total` touching `binary ten integer by six` is not an instruction by itself
  - `integer by six` touching `baseten integer` is not an output route by itself

## Detached Families And Current Role Classifications

### Top ribbon controller-like families

- `find the start`
  - current role: likely meta-instruction
- `find the start nine`
  - current role: likely meta-instruction
- `find the start nine go`
  - current role: likely meta-instruction
- `go ahead`
  - current role: likely meta-instruction

### Upper-left / detached extraction-like families

- `get ones`
  - current role: likely secondary extraction layer
- `three ten hundred`
  - current role: likely local but unsolved clue family
- `three ten hundreds`
  - current role: likely local but unsolved clue family

## What This Pass May Treat As Locked

- the lower-right component is the strongest exact local engine
- `step or X` is the strongest local exact shell
- `90` and `2` are only local/provisional outputs
- top ribbon is currently better modeled as detached meta-language than as missing overlap
- `get ones` is currently better modeled as detached extraction language than as missing overlap

## What Remains Open Globally

- whether the top ribbon can act as a non-overlap controller with an objective referent map
- whether detached extraction language can act on a chosen lower-right engine output with a licensed handoff
- whether any full controller -> engine -> extraction pipeline is grounded enough to beat the current `no answer yet` state

## Pass Discipline

- This pass should not reopen lower-right local syntax unless a controller or extraction hypothesis strictly requires it.
- This pass should distinguish four relation types:
  - exact overlap/subphrase composition
  - meta/control relation
  - extraction relation
  - unsupported leap
