# Detached Fragment Roles

This file revisits the detached families only after the lower-right syntax pass.

Rule used here:

- no forced exact bridge
- classify each detached family by role under the current evidence

## `find the start`

- classification:
  - likely meta-instruction
- why:
  - exact phrase is clean and directive
  - it still does not exact-attach to the lower-right component
  - that makes it more natural as a read-order or entry-point hint than as a missing overlap

## `find the start nine`

- classification:
  - likely meta-instruction
- why:
  - the added `nine` may indicate a start value, a start location, or a clue index
  - but the disconnected `nine` problem remains if one tries to force it into the lower-right branch

## `find the start nine go`

- classification:
  - likely meta-instruction
- why:
  - exact and directive
  - strongest near-miss to lower-right `go integer by six` still fails by disconnected `go`

## `go ahead`

- classification:
  - likely meta-instruction
- why:
  - exact control-language shell on the top side
  - still detached from the lower-right component

## `get ones`

- classification:
  - likely secondary extraction layer
- why:
  - it looks like digit/place-value language
  - it still does not exact-attach to the lower-right component
  - it is more plausibly a second-stage extraction hint than raw noise

## `three ten hundred`

- classification:
  - likely local but unsolved clue family
- why:
  - exact and number-like
  - not proven to plug into the lower-right parse
  - still stronger than noise, but weaker than a clear meta-instruction

## `three ten hundreds`

- classification:
  - likely local but unsolved clue family
- why:
  - same as above, with pluralized `hundreds`

## Detached-Family Takeaway

- The top ribbon now looks more meta than missing-overlap.
- The upper-left / `get ones` material still looks clue-bearing, but as a secondary layer rather than a missing exact attachment.
- The current evidence does not support forcing any of these detached families into the lower-right syntax.
